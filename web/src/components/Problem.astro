---
const incidents = [
  {
    icon: '💥',
    name: 'colors + faker',
    year: 'January 2022',
    description: 'Maintainer intentionally corrupted packages with infinite loops, breaking thousands of projects overnight.',
    stat: '25M+ weekly downloads affected'
  },
  {
    icon: '🎯',
    name: 'event-stream',
    year: 'November 2018',
    description: 'Maintainer handed off project to attacker who injected cryptocurrency-stealing malware.',
    stat: '2M+ downloads with malware'
  },
  {
    icon: '🕳️',
    name: 'left-pad',
    year: 'March 2016',
    description: '11-line package was unpublished, breaking React, Babel, and thousands of builds worldwide.',
    stat: 'Global npm outage'
  }
]
---

<section class="py-16 border-t border-[#1a1a1f] animate-fade-in-up stagger-2 opacity-0">
  <span class="text-xs font-semibold tracking-widest uppercase text-blue-400 mb-4 block">
    The Problem
  </span>

  <h2 class="text-2xl md:text-3xl font-bold mb-4">
    Packages are ticking time bombs
  </h2>

  <p class="text-zinc-400 mb-8 max-w-2xl">
    Every year, critical packages get abandoned, compromised, or sabotaged.
    By the time you notice, it's already breaking production.
  </p>

  <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
    {incidents.map((incident) => (
      <div class="bg-[#111113] border border-[#1a1a1f] rounded-xl p-6 card-hover">
        <div class="flex items-center gap-3 mb-4">
          <div class="w-10 h-10 bg-red-500/10 rounded-lg flex items-center justify-center text-lg">
            {incident.icon}
          </div>
          <div>
            <div class="font-semibold font-mono text-sm">{incident.name}</div>
            <div class="text-xs text-zinc-500">{incident.year}</div>
          </div>
        </div>

        <p class="text-sm text-zinc-400 leading-relaxed mb-4">
          {incident.description}
        </p>

        <span class="inline-block text-xs font-medium text-red-400/80 bg-red-500/10 px-2 py-1 rounded">
          {incident.stat}
        </span>
      </div>
    ))}
  </div>

  <!-- Blind spot callout -->
  <p class="text-sm text-zinc-500 mt-8 max-w-2xl">
    These weren't security vulnerabilities. They were maintainer problems—a blind spot in traditional tooling.
  </p>
</section>
