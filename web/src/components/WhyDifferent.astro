---
---

<section class="py-16 border-t border-[#1a1a1f] animate-fade-in-up stagger-4 opacity-0">
  <span class="text-xs font-semibold tracking-widest uppercase text-blue-400 mb-4 block">
    Predictive Intelligence
  </span>

  <h2 class="text-2xl md:text-3xl font-bold mb-4">
    The first tool that <span class="gradient-text">predicts</span> package health
  </h2>

  <p class="text-zinc-400 mb-10 max-w-2xl">
    Security scanners alert you to known CVEs. We predict abandonment 6-12 months before it happens. Different problem, different solution.
  </p>

  <!-- Positioning Grid with Headers -->
  <div class="max-w-2xl mx-auto">
    <!-- Column Headers -->
    <div class="grid grid-cols-[80px_1fr_1fr] gap-3 mb-2">
      <div></div>
      <div class="text-center text-xs text-zinc-500 font-medium tracking-wide uppercase">Security</div>
      <div class="text-center text-xs text-zinc-500 font-medium tracking-wide uppercase">Maintenance</div>
    </div>

    <!-- Row 1: Predictive -->
    <div class="grid grid-cols-[80px_1fr_1fr] gap-3 mb-3">
      <div class="flex items-center justify-end pr-3">
        <span class="text-xs text-zinc-500 font-medium tracking-wide uppercase">Predictive</span>
      </div>

      <!-- Predictive + Security (empty) -->
      <div class="bg-[#111113] border border-[#1a1a1f] rounded-xl p-5 min-h-[120px] flex items-center justify-center">
        <span class="text-zinc-600 text-sm italic">emerging space</span>
      </div>

      <!-- Predictive + Maintenance (PkgWatch - highlighted) -->
      <div class="relative bg-gradient-to-br from-blue-500/10 to-purple-500/10 border-2 border-blue-500/50 rounded-xl p-5 min-h-[120px] flex flex-col items-center justify-center shadow-lg shadow-blue-500/10">
        <span class="text-xl font-bold text-white mb-1">PkgWatch</span>
        <span class="text-xs text-blue-300">We created this category</span>
        <!-- Glow effect -->
        <div class="absolute inset-0 rounded-xl bg-blue-500/5 blur-xl -z-10"></div>
      </div>
    </div>

    <!-- Row 2: Reactive -->
    <div class="grid grid-cols-[80px_1fr_1fr] gap-3">
      <div class="flex items-center justify-end pr-3">
        <span class="text-xs text-zinc-500 font-medium tracking-wide uppercase">Reactive</span>
      </div>

      <!-- Reactive + Security -->
      <div class="bg-[#111113] border border-[#1a1a1f] rounded-xl p-5 min-h-[120px] flex flex-wrap items-center justify-center gap-2">
        <span class="text-xs bg-zinc-800 text-zinc-400 px-2 py-1 rounded">Snyk</span>
        <span class="text-xs bg-zinc-800 text-zinc-400 px-2 py-1 rounded">Dependabot</span>
        <span class="text-xs bg-zinc-800 text-zinc-400 px-2 py-1 rounded">npm audit</span>
      </div>

      <!-- Reactive + Maintenance -->
      <div class="bg-[#111113] border border-[#1a1a1f] rounded-xl p-5 min-h-[120px] flex flex-wrap items-center justify-center gap-2">
        <span class="text-xs bg-zinc-800 text-zinc-400 px-2 py-1 rounded">deps.dev</span>
        <span class="text-xs bg-zinc-800 text-zinc-400 px-2 py-1 rounded">libraries.io</span>
      </div>
    </div>
  </div>

  <!-- Bottom callout -->
  <p class="text-center text-sm text-zinc-500 mt-8 max-w-lg mx-auto">
    Your security tools find known vulnerabilities. PkgWatch finds the packages that will cause problems next year.
  </p>
</section>
