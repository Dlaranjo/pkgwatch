{
  "name": "pkgwatch-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "PkgWatch - Dependency Health Intelligence Platform",
  "workspaces": [
    "packages/*",
    "cli",
    "action"
  ],
  "scripts": {
    "build": "npm run build -w @pkgwatch/api-client && npm run build -w @pkgwatch/cli && npm run build -w @pkgwatch/action",
    "build:api-client": "npm run build -w @pkgwatch/api-client",
    "test": "npm run test --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
